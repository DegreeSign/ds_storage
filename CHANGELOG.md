# Changelog

All notable changes to this project will be documented in this file.

## Version 1.1.4
- Exported `cryptoKey`, which derives an AES-GCM `CryptoKey` from a string, so consumers can derive keys without configuring `configureStorage`.

## Version 1.1.2
- Renamed the config parameter `scPrefix` to `localStoragePrefix`.
